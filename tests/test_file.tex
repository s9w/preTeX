\documentclass[12pt]{article}
\usepackage{amsmath}
\title{preTeX test file
\begin{document}

\maketitle
% pretex braket_expanding

\section{inline}
This is an epic test file. This should not be translated: x. But some of these: $x. x. y..
b_i.. a. \varphi_i..$. d$\frac a b$. $u_tt$. $a -> b$. $a _ tt ^ a,b$.
$a _ tt ^   a,b$
a$x -> y\$x -> y$b
$a -> b$
$a*b$
$a* b$
$a<bra|mid|ket>b$
$a<bra|ket>b$
$a |\ket> b$
$a <bra| b$
$a |k><bra| b$
$= { x | x>0 }$

\section{single and multiple labels}
\begin{eqnarray}
    x.
    \Sigma(R) = \Sigma_{0} \left(\frac{R}{R_{\rm c}}\right)^{-q}{\rm exp}\left[-\left(\frac{R}{R_{\rm c}}\right)^{2-q}\right],
    a x. a \label{eq_test} x x. x
\end{eqnarray}

\begin{eqnarray}
    x.
    \Sigma(R) = \Sigma_{0} \left(\frac{R}{R_{\rm c}}\right)^{-q}{\rm exp}\left[-\left(\frac{R}{R_{\rm c}}\right)^{2-q}\right],
    a x. a \label{eq_test} x x. x  x x. x \label{eq_test} x x. x
\end{eqnarray}

\section{markdown}
- this is markdown list
- should still work! $a*b$ but not $a^*$ and not here... $x^2 <1|2|3>$.

\section{envs}
\begin {align}
a = b \\
x = y
\end{align}

\begin {align}
a = x = b \\
x = y
\end{align}

\begin {align}
a = b \\
x &= y
\end{align}

\begin {align}
a = b
x = y
\end{align}

\begin {align*}
a &= b.
x &= y
\end{align*}

newline
\end{document}